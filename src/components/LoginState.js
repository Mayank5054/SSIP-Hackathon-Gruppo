import React from "react";

const LoginState = ({ text }) => {
    return (
        <div className='fixed shadow-mine rounded-md mt-5 p-1 bg-primary-900 text-secondary-700 '>
            {text}
        </div>
    );
};

export default LoginState;
